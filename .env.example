# AI Resume Analyzer Configuration
# Since this system runs completely offline, no external API keys are required by default.
# If you wish to extend the application to support external LLMs in the future,
# you can define the keys below:

OPENAI_API_KEY=your_openai_key_here
GEMINI_API_KEY=your_gemini_key_here
PORT=8501
DEBUG=False
